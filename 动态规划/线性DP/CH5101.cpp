#include <bits/stdc++.h>
using namespace std;

const int maxn = 3e3 + 10;
int a[maxn], b[maxn], dp[maxn], dex[maxn];
#define rep(x, y) for(int i = x; i < y; i++)

int main()
{
	int n;
	scanf("%d", &n);
	rep(1, n+1) scanf("%d", &a[i]);
	rep(1, n+1) scanf("%d", &b[i]);
	dp[0] = 0;
	rep(1, n+1){
		return 0;
	}
}
