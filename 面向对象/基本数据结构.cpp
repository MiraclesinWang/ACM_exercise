#include <bits/stdc++.h>

map:
	mp.clear();
	mp['a'] = 1;
	mp.find('b') == mp.end()
vector:
	v.push_back(1);
	l = v.end() - v.begin();
	v[10];
	v.erase(v.begin()+1, v.end());
	v.clear();
list:
	l.push_back();
	l.push_front();
	for(auto it = l.begin(); it != l.end(); it++) printf("%d\n", *it);
	l.clear();
priority_queue <int, vector<int>, greater<int> > pq:
	pq.push();
	pq.pop();
	pq.top();
int m = unique(m+1, m+1+n) - m - 1;
int dex = lower_bound(m+1, m+1+n, v) - m - 1;
